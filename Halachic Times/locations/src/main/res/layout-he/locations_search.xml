<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:gravity="center_vertical"
    android:layoutDirection="ltr"
    android:orientation="horizontal">

    <ImageButton
        android:id="@+id/my_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/title_activity_location"
        android:src="@android:drawable/ic_menu_mylocation"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingLeft="2dp">

        <ImageView
            android:id="@+id/search_close_btn"
            android:layout_width="@dimen/search_close_size"
            android:layout_height="@dimen/search_close_size"
            android:contentDescription="@string/cancel"
            android:padding="@dimen/search_close_padding"
            android:src="@drawable/ic_clear"/>

        <EditText
            android:id="@+id/search_src_text"
            style="@style/Widget.Location.Search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@android:string/search_go">

            <requestFocus/>
        </EditText>
    </LinearLayout>

</LinearLayout>