<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.compass.jewish">

    <application
        android:name="com.github.compass.jewish.CompassApplication"
        android:allowBackup="true"
        android:appCategory="maps"
        android:backupAgent="com.github.times.compass.backup.CompassBackupAgent"
        android:fullBackupContent="@xml/full_backup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompassApp.DayNight"
        tools:replace="android:name">
        <activity
            android:name="com.github.compass.jewish.CompassActivity"
            android:label="@string/title_activity_compass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value="com.github.times.location.LocationActivity" />
        </activity>
        <activity
            android:name="com.github.times.compass.preference.CompassPreferenceActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.github.compass.jewish.CompassActivity"
            android:theme="@style/Theme.CompassApp.Settings" />
        <activity
            android:name="com.github.compass.jewish.LocationActivity"
            android:label="@string/title_activity_location"
            android:parentActivityName="com.github.compass.jewish.CompassActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <meta-data
            android:name="com.google.android.backup.api_key"
            android:value="@string/google_backup_api_key" />
    </application>

</manifest>